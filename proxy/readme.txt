HTTP-Proxy
Умеет:
1. работать как прокси (в несколько потоков)
2. блокировать рекламу (для этого нужно прописать регексы в json файл и при запуске -b имя.json)

Не умеет:
1. HTTPS

Параметры запуска:
-c количество потоков
-p порт, который будет слушать
-t таймоут на коннект
-v доп инфа
-b блэклист